version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - .:/app
      - chroma_data:/app/chroma_db
      - audit_data:/app/audit_logs
    restart: always

volumes:
  chroma_data:
  audit_data:
